services:
  postgres:
    image: postgres:16-alpine
    container_name: schedly-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: schedly
      POSTGRES_PASSWORD: schedly_dev
      POSTGRES_DB: schedly
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U schedly -d schedly"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Uncomment when needed: docker compose up pgadmin
  # pgadmin:
  #   image: dpage/pgadmin4:latest
  #   container_name: schedly-pgadmin
  #   restart: unless-stopped
  #   ports:
  #     - "5050:80"
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: admin@schedly.dev
  #     PGADMIN_DEFAULT_PASSWORD: admin
  #   volumes:
  #     - pgadmin_data:/var/lib/pgadmin
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

volumes:
  postgres_data:
  # pgadmin_data:
